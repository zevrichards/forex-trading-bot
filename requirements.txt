pytest>=8.0
fastapi>=0.110
uvicorn>=0.29
pydantic>=2.6
httpx>=0.27  # needed by fastapi.testclient.TestClient (tests/test_webhook_receiver.py)
gspread>=6.0
google-auth>=2.29
ctrader-open-api>=0.9.2
# ctrader-open-api's request-building is used/tested (order_execution.py);
# the actual network connection isn't wired up yet — see that module's
# docstring for why.
